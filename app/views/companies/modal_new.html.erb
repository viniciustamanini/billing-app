<turbo-frame id="modal-frame">
  <h1 class="text-2xl font-bold mb-6">New Company</h1>
  <button data-action="click->modal#close" class="btn-primary">Close</button>
  <%= render "form" %>
</turbo-frame>
