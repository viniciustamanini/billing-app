<turbo-frame id="modal-frame">
  <div class="relative">
    <h1 class="text-2xl font-bold mb-6"><%= t("register_new_company") %></h1>
    <button
      class="absolute top-2 right-2 text-gray-500 hover:text-gray-700"
      data-action="click->modal#close"
    >
      <i class="material-symbols-outlined text-4xl">close_small</i>
    </button>
  </div>

  <%= render "form" %>
</turbo-frame>
