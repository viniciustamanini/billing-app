<h1>Dasboard</h1>
<div class="dashboard">
  <div class="content-card-row">
    <div class="white-card">
      <div class="card-content">
        <h2>Recent Users</h2>
        <ul>
          <% @users.each do |user| %>
            <li><%= user.id %>
              (<%= user.email %>)</li>
          <% end %>
        </ul>
      </div>
    </div>

    <div class="white-card">
      <div class="card-content">
        <h2>Recent Users</h2>
        <ul>
          <% @users.each do |user| %>
            <li><%= user.id %>
              (<%= user.email %>)</li>
          <% end %>
        </ul>
      </div>
    </div>

    <div class="gold-card">
      <div class="card-content">
        <h2>Recent Users</h2>
        <ul>
          <% @users.each do |user| %>
            <li><%= user.id %>
              (<%= user.email %>)</li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>
